import { AppShell } from "@bookflow/ui";

export default function HomePage() {
  return (
    <AppShell
      eyebrow="Customer Web"
      title="Book your time."
      description="The customer-facing BookFlow application is bootstrapped and ready for discovery, availability and booking features."
    />
  );
}
