import { AppShell } from "@bookflow/ui";

export default function HomePage() {
  return (
    <AppShell
      eyebrow="Business Web"
      title="Run every appointment."
      description="The tenant operations application is bootstrapped for calendars, bookings, queues, staff and business configuration."
    />
  );
}
