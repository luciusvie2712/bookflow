import { AppShell } from "@bookflow/ui";

export default function HomePage() {
  return (
    <AppShell
      eyebrow="Admin Web"
      title="See the whole platform."
      description="The platform administration application is bootstrapped for businesses, users, subscriptions and operational controls."
    />
  );
}
